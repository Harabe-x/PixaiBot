<Window x:Class="PixaiBot.UI.View.LogWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PixaiBot.UI.View"
        mc:Ignorable="d"
        Title="LogWindowView" Height="800" Width="400">
        
        <Window.Resources>
        </Window.Resources>
    
        <Border>
                <Grid>
                    <ScrollViewer Name="ScrollViewer" ScrollChanged="ScrollViewer_OnScrollChanged" >
                            <TextBox  Name="TextBox" Text="{Binding Log}" ></TextBox>
                    </ScrollViewer>
                </Grid> 
        </Border>
        
</Window>
