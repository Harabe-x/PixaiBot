<UserControl x:Class="PixaiBot.UI.View.LogAccountInfoControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PixaiBot.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Style="{StaticResource TitleText}" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Account info logger" />
                <TextBlock Style="{StaticResource SecondaryText}" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Here you can log account to text file " />
            </StackPanel>

        </Grid>
            
    </Grid>
</UserControl>
